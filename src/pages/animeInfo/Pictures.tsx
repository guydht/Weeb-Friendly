import React, { Component } from "react";
import { AnimeInfoProps } from "../AnimeInfo";

export default class Pictures extends Component<AnimeInfoProps> {
    render() {
        return (
            <div></div>
        )
    }
}