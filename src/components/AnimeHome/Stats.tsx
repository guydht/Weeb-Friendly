import React, { Component } from "react";
import { AnimeProps } from "../AnimePage";

export default class Stats extends Component<AnimeProps> {
    render() {
        return (
            <div></div>
        )
    }
}