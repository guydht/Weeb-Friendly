import React, { Component } from "react";
import { AnimeProps } from "../AnimePage";

export default class Download extends Component<AnimeProps> {
    render() {
        return (
            <div></div>
        )
    }
}