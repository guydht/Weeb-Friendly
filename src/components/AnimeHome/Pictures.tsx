import React, { Component } from "react";
import { AnimePageProps } from "../AnimePage";

export default class Pictures extends Component<AnimePageProps> {
    render() {
        return (
            <div></div>
        )
    }
}